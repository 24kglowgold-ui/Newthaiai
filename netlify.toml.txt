[build]
  # The publish directory is the folder Netlify will serve (where index.html is located).
  publish = "/"
  # The functions directory is where Netlify will find and deploy your serverless functions.
  functions = "netlify/functions"
  # Since this is a static site with a serverless function, no explicit build command is strictly needed
  # if 'npm install' is run before the function is built, but we can specify the build steps.
  command = "npm install"

# Global settings for all contexts
[functions]
  node_bundler = "esbuild"
  # Timeout for the chat function (max 26 seconds for standard accounts)
  timeout = 15

# Environment variables for production context (required for the proxy function)
# NOTE: The GEMINI_API_KEY should be set securely in the Netlify UI under Site Settings > Environment Variables
# The function 'gemini-chat-proxy.js' relies on this variable.
[context.production.environment]
  # GEMINI_API_KEY = "YOUR_GEMINI_API_KEY_HERE" # Placeholder - Set securely in Netlify UI